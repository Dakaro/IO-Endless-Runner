<% 
    const testFunc = () => {
        console.log('function works')
        const tempDiv = document.createElement('div')
        tempDiv.innerHTML = "Temp div for tests"
        document.body.appendChild(tempDiv)
    }   
 %> 